[phases.setup]
aptPkgs = ["python3", "python3-venv", "python3-pip"]

[phases.install]
cmds = [
  "python3 -m venv /app/.venv",
  "/app/.venv/bin/pip install -r server/requirements.txt"
]

[start]
cmd = "cd server && /app/.venv/bin/gunicorn app:app"

